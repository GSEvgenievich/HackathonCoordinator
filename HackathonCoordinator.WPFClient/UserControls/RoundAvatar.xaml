<UserControl x:Class="HackathonCoordinator.WPFClient.UserControls.RoundAvatar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             x:Name="Root">

    <Border x:Name="MainBorder"
            Width="{Binding Size, ElementName=Root}"
            Height="{Binding Size, ElementName=Root}"
            CornerRadius="{Binding Size, ElementName=Root}"
            BorderBrush="{Binding BorderBrush, ElementName=Root}"
            BorderThickness="{Binding BorderThickness, ElementName=Root}">

        <Border.Effect>
            <DropShadowEffect ShadowDepth="3"
                              Opacity="0.4"
                              BlurRadius="6"/>
        </Border.Effect>

        <Ellipse>
            <Ellipse.Fill>
                <ImageBrush ImageSource="{Binding Source, ElementName=Root}" 
                            Stretch="UniformToFill"/>
            </Ellipse.Fill>
        </Ellipse>
    </Border>
</UserControl>